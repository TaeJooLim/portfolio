계약 상세내용 테이블

create table contract_detail(
	contD_id int auto_increment not null primary key,	// PK
	contD_doc_num_fk varchar(30) not null,		// 문서번호 (참조키로 사용)
	contD_p_name varchar(100) not null,			// 품명
	contD_p_unit varchar(5) not null,			// 단위
	contD_p_amount int not null,			// 수량
	contD_p_sell int not null				// 단가
);



create table contract_detail(
	contD_id int auto_increment not null primary key,
	contD_doc_num_fk varchar(30) not null,
	contD_p_name varchar(100) not null,
	contD_p_unit varchar(5) not null,
	contD_p_amount int not null,
	contD_p_sell int not null
);

2-1. html에는 부가세, 공급가액 컬럼이 추가되어야 함.
2-2. 부가세와 공급가액은 JavaScript를 이용해서 계산해서 표시