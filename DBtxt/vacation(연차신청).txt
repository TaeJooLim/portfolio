연차신청 테이블

create table vacation (
	offDate char(10) not null,		// 휴가시작일 (input type ="date")
	offType char(1) not null,		// 휴가종류 (연차,반차,휴직)
	offConfirm char(1) default 'N'	// 결재여부 (부서장이 결재)
	
	emp_id_fk int not null,		// employee 테이블 조인
);