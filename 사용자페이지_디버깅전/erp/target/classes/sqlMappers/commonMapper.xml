<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="common">

	
	<select id="getDepartmentList" resultType="employeevo">
		SELECT depart_code AS empDepartment, depart_name AS empDepartmentName FROM department
	</select>
	<select id="getPositionList" resultType="employeevo">
		SELECT position_code AS empPosition, position_name AS empPositionName FROM position
	</select>
	<select id="getHeadInfo" parameterType="String" resultType="employeevo">
		SELECT 
			emp_id			AS empId,
			emp_department	AS empDepartment,
			depart_name		AS empDepartmentName,
			emp_position	AS empPosition,
			position_name 	AS empPositionName 
		FROM employee 		LEFT JOIN department ON emp_department = depart_code 
						  	LEFT JOIN position ON emp_position = position_code  
						  	WHERE emp_department = #{empDepartment} AND emp_headcheck = 'Y'
	</select>
	<select id="getDepartmentOne" parameterType="String" resultType="String">
		SELECT depart_name FROM department WHERE depart_code = #{empDepartment}
	</select>
</mapper>